-- =====================================================================
-- รูปปกพื้นหลังผูกกับตัวละคร (แสดงเมื่อผู้เล่นเลือกตัวละครนั้น)
-- รันใน Supabase SQL Editor (รันซ้ำได้)
-- Admin ตั้งค่า: Admin Panel > ตัวละคร > รูปปก (ต่อตัว)
-- =====================================================================

alter table public.game_characters
  add column if not exists cover_url text;
